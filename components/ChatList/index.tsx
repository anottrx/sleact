import React, { FC, useCallback } from 'react';
import { ChatZone } from './styles';

interface Props {
  chat: string;
}

const ChatList: FC<Props> = ({ chat }) => {
  return <ChatZone>section</ChatZone>;
};

export default ChatList;
